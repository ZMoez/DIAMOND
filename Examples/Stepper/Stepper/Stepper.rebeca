reactiveclass Stepper (3){
    statevars {
        int x;
    }

    Stepper() {
        x = 0;
        self.step();
    }

    msgsrv step() {
        if (x < 6) 
        {
            if (x == 3 && ?(true, false)) 
                self.reset();
            else 
                x = x + 1;    
        } 
        else 
        {  
            self.reset();
        }
        self.step();
    }
    
    void reset(){
        x = 0;
    }
}

main {
    Stepper stepper():();
}

